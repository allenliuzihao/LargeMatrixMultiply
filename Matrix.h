#pragma once

// matrix of M rows and N columns, with elements of type T.
template <typename T, typename M, typename N>
class Matrix
{
public:

private:
    bool columnMajor = false; // default to row major.
    //Vector<T, N> m_data[M];
};